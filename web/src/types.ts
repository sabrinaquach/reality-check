// The engine is the single source of truth for these shapes; re-exported as
// types only, so nothing from ../spike ends up in the browser bundle.
export type { Band, Pillar, PillarItem, Priority, RealityCheck, Route } from "../../spike/src/types.ts";
